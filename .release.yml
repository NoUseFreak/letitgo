homebrew:
  - name: release
    description: GitTeams gives you insight into multiple repositories at once.
    homepage: https://github.com/NoUseFreak/release
    url: https://github.com/NoUseFreak/release/releases/download/{{ .Version }}/darwin_amd64.zip
    version: "{{ .Version }}"
    tap:
      url: git@github.com:NoUseFreak/homebrew-brew.git
    test: system "#{bin}/{{ .Name }} -h"
